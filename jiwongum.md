# 어르신 지원금 · 업데이트 릴리스 기획서

- **대상**: `com.jiwongum.app` — **프로덕션 배포 완료된 라이브 앱**
- **성격**: 신규 출시가 아니라 **기존 앱의 대규모 리브랜딩 업데이트**
- **선행 문서**: `SPEC.md`(웹) / `SPEC-APP.md`(앱 기획) / `SPEC-APP-RN.md`(Expo 구현)
- **저장 위치**: 저장소 루트 `RELEASE-PLAN.md`
- **이 문서의 역할**: 위 세 문서를 **어떤 순서로, 어떤 안전장치와 함께 배포하는가**

---

## 0. 이 문서가 따로 필요한 이유

앞선 세 문서는 "무엇을 만드는가"를 다룬다. 그러나 이미 프로덕션에 사용자가 있는 앱이므로, 같은 내용이라도 **배포 순서를 틀리면 기존 사용자를 잃는다.**

라이브 앱 업데이트에서 새로 생기는 제약은 네 가지다.

| 제약 | 내용 |
|---|---|
| **하위 호환** | 웹은 즉시 바뀌지만 앱은 사용자가 업데이트해야 바뀐다. 그 사이 **구버전 앱 + 신버전 웹** 조합이 존재한다 |
| **Play Integrity** | 무결성 검증이 걸려 있으면 개발 빌드가 전부 차단된다. 이번 작업 자체가 막힌다 |
| **브랜드 전환 충격** | 아이콘과 이름이 동시에 바뀌면 이 타겟은 "앱이 없어졌다"고 인식한다 |
| **되돌릴 수 없음** | Play는 이미 업데이트된 사용자를 이전 버전으로 되돌리지 못한다 |

---

## 1. 최우선 원칙 — 웹은 즉시, 앱은 점진

### 1.1 반드시 인지할 것

웹뷰 앱이므로 `senior.zucca100.com`을 바꾸는 순간, **아직 업데이트하지 않은 구버전 앱 사용자도 새 웹을 본다.** 이것은 장점이자 함정이다.

- **장점**: 웹 개편(`SPEC.md`)은 스토어 심사 없이 즉시 전체 사용자에게 반영된다. 개선의 90%가 여기 있다.
- **함정**: 새 웹이 구버전 앱 셸과 충돌하면, 업데이트하지 않은 사용자의 앱이 깨진다.

### 1.2 신규 웹이 지켜야 할 하위 호환 규칙

| 항목 | 규칙 | 이유 |
|---|---|---|
| 전화번호 | `tel:` 링크와 **함께 번호를 텍스트로 노출** | 구버전 앱은 `tel:` 핸들러가 없어 아무 반응이 없다 |
| 하단 고정 요소 | 웹에서 화면 하단에 아무것도 고정하지 않는다 | 구버전 앱의 하단 탭바와 겹친다 |
| 하단 여백 | 본문 최하단에 `padding-bottom: 100px` 확보 | 탭바에 콘텐츠가 가리는 것 방지 |
| 외부 링크 | `target="_blank"` 대신 동일 창 이동 | 구버전 앱은 새 창 처리가 없어 링크가 죽는다 |
| URL 경로 | 기존 경로를 **삭제하지 말고 301 리다이렉트** | 구버전 앱·푸시·카톡 공유 링크가 옛 경로를 가리킨다 |
| 앱 감지 | `window.IS_NATIVE_APP` 부재를 정상 케이스로 처리 | 구버전 앱은 이 플래그를 주입하지 않는다 |

**리다이렉트 맵을 반드시 만든다.** 현재 사이트의 전 URL을 수집해 신규 구조로 매핑하고, 하나도 404가 나지 않게 한다. 카카오톡 채널로 이미 뿌린 링크들이 살아 있다.

---

## 2. Play Integrity API 취급

### 2.1 먼저 목적을 재정의한다

이 앱은 로그인도 결제도 없는 무료 정보 앱이다. 무결성 검증을 붙인 실질적 목적은 **AdMob 무효 트래픽 차단**(에뮬레이터·자동화 클릭)일 가능성이 높다.

그렇다면 **검증 실패 시 막아야 할 것은 앱이 아니라 광고다.**

| 판정 | 조치 |
|---|---|
| `PLAY_RECOGNIZED` + `MEETS_DEVICE_INTEGRITY` | 정상. 광고 노출 |
| `UNRECOGNIZED_VERSION` | **광고만 끈다.** 콘텐츠는 정상 제공 |
| `UNEVALUATED` / 판정 실패 / 오류 | **광고만 끈다.** 콘텐츠는 정상 제공 |
| 어떤 경우에도 | **앱 사용 자체를 차단하지 않는다** |

**이 타겟에서 하드 게이트는 위험하다.** 60대 이상 사용자는 구형 기기와 오래된 Play 서비스·Play 스토어 버전을 쓰는 비율이 높아, `PLAY_STORE_VERSION_OUTDATED` 같은 오탐이 일반 사용자보다 훨씬 자주 발생한다. 그때 앱이 "무결성 검증에 실패했습니다"를 띄우고 멈추면, 그 사용자는 앱을 삭제한다. 되돌릴 방법이 없다.

현재 코드가 하드 게이트로 되어 있다면 **이번 업데이트에서 소프트 게이트로 전환하는 것을 1순위 과제로 둔다.**

### 2.2 개발 중 반드시 막히는 지점

`appRecognitionVerdict`는 앱의 서명 인증서와 패키지명이 Google Play 기록과 일치할 때만 `PLAY_RECOGNIZED`를 반환한다. 즉:

- `expo run:android`, `expo-dev-client` 로컬 빌드 → **디버그 키 서명 → 항상 `UNRECOGNIZED_VERSION`**
- EAS `preview` 프로파일의 APK 직접 설치 → **Play를 거치지 않음 → `UNRECOGNIZED_VERSION`**
- Play 내부 테스트 트랙에 업로드된 빌드 → **정상 `PLAY_RECOGNIZED`**

**따라서 개발 우회 플래그가 없으면 이번 리팩터링 작업 자체가 불가능하다.**

```ts
// 무결성 게이트는 릴리스 빌드에서만 동작하게 한다
const shouldEnforceIntegrity = !__DEV__ && !Constants.expoConfig?.extra?.skipIntegrity;
```

`app.config.js`에서 EAS 프로파일별로 `skipIntegrity`를 주입하고, **`production` 프로파일에서만 false**로 둔다. 이 플래그가 프로덕션 빌드에 새어 들어가지 않도록 릴리스 체크리스트에 포함한다.

### 2.3 할당량

Play Integrity의 기본 할당량은 하루 토큰 요청 1만 건, 디코딩 1만 건이며, 기기당 요청 빈도에도 분당 제한이 있다. 초과하면 `TOO_MANY_REQUESTS`가 반환되므로 지수 백오프로 재시도하고, 사용자 규모가 커지면 할당량 증액을 신청한다.

**호출 빈도를 줄인다.** 앱 실행마다 검증할 필요가 없다.

- 검증 시점: **앱 콜드 스타트 1회**만
- 결과 캐시: 24시간
- 화면 전환·탭 이동마다 재검증 금지

무료 정보 앱에서 실행마다 무결성 토큰을 받으면 할당량만 태우고 얻는 것이 없다.

### 2.4 오류 문구 재작성

기존 문구가 기술 용어라면 전부 바꾼다. 이 타겟은 "무결성", "검증", "위변조"를 읽지 못한다.

| 상황 | 금지 문구 | 사용 문구 |
|---|---|---|
| 판정 실패 | 앱 무결성 검증에 실패했습니다 | (아무것도 띄우지 않는다. 광고만 조용히 끈다) |
| Play 서비스 구버전 | PLAY_SERVICES_VERSION_OUTDATED | 스토어 앱을 최신으로 바꿔주시면 더 잘 동작해요 |
| 재시도 필요 | 일시적 오류 | 잠시 뒤에 다시 열어보세요 |

원칙: **무결성 관련 화면을 사용자에게 보여주지 않는 것이 최선이다.** 조용히 광고만 끄고, 사용자는 아무것도 모르는 채 지원금 정보를 계속 본다.

### 2.5 iOS

Play Integrity는 Android 전용이다. iOS 측에 대응 로직(App Attest 등)이 없다면 지금 만들 필요 없다. 이 앱의 위험도에서는 과잉이다. 다만 광고 정책은 양 플랫폼 동일하게 적용한다.

---

## 3. 4단계 릴리스 계획

한 번에 전부 배포하지 않는다. 각 단계는 **되돌릴 수 있는 지점**에서 끊는다.

### Phase 0 — 준비 (배포 없음)

- [ ] 현재 프로덕션 지표 스냅샷: DAU, 리텐션, 평균 체류, AdMob CTR·노출, 스토어 평점
  → **개편 후 비교 기준이 없으면 개선인지 개악인지 알 수 없다.** 이걸 안 찍고 시작하는 실수가 가장 흔하다.
- [ ] 현재 사이트 전 URL 수집 → 리다이렉트 맵 작성
- [ ] Play Integrity 현재 구현 위치·게이트 방식 확인 (하드/소프트)
- [ ] `skipIntegrity` 개발 플래그 도입
- [ ] KIPRIS 상표 검색 → 서비스명 최종 확정
- [ ] 타겟 세분화 방향 확정 (60대 이상 여성 조준 강도 — 미결)
- [ ] 스테이징 도메인 준비 (`stg.senior.zucca100.com`, `robots: noindex`)

### Phase 1 — 웹 개편 (앱 배포 없음) ★ 가장 큰 레버

`SPEC.md` 전체를 배포한다. **스토어 심사 없이 전체 사용자에게 즉시 반영된다.**

- 반드시 §1.2 하위 호환 규칙을 지킨 상태로 배포
- 전화번호를 텍스트로도 노출 (구버전 앱 사용자 보호)
- 리다이렉트 맵 적용, 404 0건 확인
- 브랜드명은 **사이트 안에서만** `어르신 지원금`으로 교체 (앱 이름은 아직 그대로)

**배포 후 1주간 지표 관찰.** 체류·이탈이 개선되지 않으면 앱을 건드리기 전에 웹부터 다시 잡는다. 웹에서 안 되는 것은 앱에서도 안 된다.

이 단계에서 앱 이름과 사이트 이름이 잠시 불일치하지만, 이미 `지원금 모아보기` vs `시니어 서포트`로 불일치 상태이므로 악화는 아니다.

### Phase 2 — 앱 기능 업데이트 (이름·아이콘 유지)

`SPEC-APP.md` / `SPEC-APP-RN.md`의 기능만 배포한다. **브랜드는 아직 바꾸지 않는다.**

포함: 하단 탭바 4개, `tel:` 처리, 오프라인 화면, 저장함, 푸시 권한 로직, Integrity 소프트 게이트 전환, 광고 배치 교정

제외: 앱 이름, 아이콘, 스플래시

- Play 내부 테스트 → 비공개 테스트 → 단계적 출시 **5% → 20% → 50% → 100%**
- 각 단계 최소 48시간, ANR·크래시율 확인 후 진행
- 크래시율이 이전 버전 대비 상승하면 즉시 halt

**이름과 아이콘을 여기서 빼는 이유**: 기능 변경과 브랜드 변경을 섞으면, 문제가 생겼을 때 원인을 분리할 수 없다. 그리고 아이콘이 바뀌면 사용자 문의가 몰리는데, 그때 기능 버그까지 겹치면 대응이 불가능하다.

### Phase 3 — 브랜드 전환

이름·아이콘·스플래시를 교체한다. §4의 커뮤니케이션 계획과 **반드시 함께** 진행한다.

- Phase 2가 100% 출시되고 2주 이상 안정된 뒤에 시작
- 카카오톡 채널 사전 공지 (D-7, D-1)
- 인앱 1회성 전환 안내 (Phase 2 빌드에 미리 심어둔다)
- 단계적 출시 **20% → 50% → 100%** (기능 변경이 없으므로 5%는 생략 가능)
- iOS는 이름 변경이 심사 대상이므로 별도 일정으로 관리

---

## 4. 브랜드 전환 커뮤니케이션

**이 항목을 생략하면 Phase 3에서 이탈이 발생한다.** 60대 이상 사용자는 홈 화면에서 아이콘 모양과 위치로 앱을 찾는다. 이름과 아이콘이 동시에 바뀌면 앱이 사라졌다고 인식하고, 재설치를 시도하다 다른 앱을 깔거나 포기한다.

### 4.1 사전 공지 (카카오톡 채널)

**D-7**
```
[알림] 앱 이름이 바뀝니다

'지원금 모아보기'가
'어르신 지원금'으로 이름을 바꿉니다.

앱을 지우지 마세요.
그대로 두시면 자동으로 바뀝니다.

다음 주에 아이콘 모양도 함께 바뀝니다.
```

**D-1** — 같은 내용에 **바뀌기 전·후 아이콘 이미지를 나란히** 넣어 발송한다. 글보다 그림이 훨씬 정확하게 전달된다.

### 4.2 인앱 전환 안내

**Phase 2 빌드에 미리 심어둔다.** Phase 3 시점에 원격 플래그로 켠다. Phase 3 빌드에 넣으면 이미 이름이 바뀐 뒤라 늦다.

```
곧 앱 이름이 바뀝니다

  [기존 아이콘] → [새 아이콘]

'지원금 모아보기'가
'어르신 지원금'이 됩니다.

앱을 지우지 않으셔도 됩니다.
저장하신 지원금도 그대로 있습니다.

              [알겠어요]
```

- 1회만 노출하고 다시 띄우지 않는다
- 닫기 버튼은 `X`가 아니라 **`알겠어요` 텍스트 버튼**

### 4.3 전환 후

- Phase 3 배포 첫 실행 시 1회: `이름이 바뀌었어요. 저장하신 내용은 그대로 있습니다.`
- 스토어 설명 첫 줄에 2개월간 명시: `(이전 이름: 지원금 모아보기)`
- 스토어 리뷰에 "앱이 없어졌다" 문의가 올라오면 즉시 답글

---

## 5. 데이터 마이그레이션

기존 사용자가 이미 가진 데이터를 잃지 않게 한다.

| 데이터 | 위치 | 조치 |
|---|---|---|
| 지역 설정 | 웹 `localStorage` | **키 이름을 바꾸지 않는다.** 부득이하면 구키→신키 1회 복사 후 구키 유지 |
| 글자 크기 | 신규 | 기존 사용자에게는 기본값 `크게` 적용 |
| Expo 푸시 토큰 | Supabase | **테이블·토큰 유지.** 재발급 시 기존 구독자를 잃는다 |
| 저장함 | 신규 | 없음. 신규 기능 |
| 웹 캐시 | 브라우저 | 웹 개편 시 CSS/JS 파일명 해시 변경으로 강제 갱신 |

**웹 `localStorage` 키를 바꾸는 것이 가장 흔한 사고다.** 지역 설정이 초기화되면 기존 사용자는 "내 지역이 없어졌다"고 느낀다. 마이그레이션 코드를 반드시 넣는다.

```js
// 웹 부팅 시 1회
const old = localStorage.getItem('senior_region');
if (old && !localStorage.getItem('region')) {
  localStorage.setItem('region', old);   // 구키는 지우지 않는다 (구버전 앱 호환)
}
```

---

## 6. 스토어 제출 리스크

| 리스크 | 확률 | 대응 |
|---|---|---|
| **Apple 4.2 (최소 기능)** | 높음 | Phase 2에서 네이티브 기능 5종 확보 후 Phase 3 제출. 심사 노트에 명시 |
| 이름 변경 심사 지연 | 중간 | iOS는 이름 변경이 심사 대상. Android보다 일정을 앞당겨 잡는다 |
| 정부 사칭 오인 | 중간 | 아이콘·스플래시에 정부 상징 미사용, 면책 문구 상시 노출 |
| 광고 정책 위반 | 중간 | 전면광고 제거, 탭바 인접 배너 이격 (`SPEC-APP-RN.md` §7) |
| **iOS 최소 버전 상승** | **확인 필요** | SDK 56은 iOS 16.4 이상을 요구한다. 기존 사용자 중 그 미만 기기 비율을 **App Store Connect에서 먼저 확인**한다 |

마지막 항목이 특히 중요하다. 이 타겟은 구형 기기 사용 비율이 높다. 기존 사용자의 상당수가 iOS 16.4 미만이라면, 그들은 **이번 업데이트를 영영 받지 못하고 구버전에 고정된다.** 그 경우 §1.2 하위 호환 규칙이 임시 조치가 아니라 영구 요건이 된다.

---

## 7. 롤백 계획

**Play는 이미 업데이트를 받은 사용자를 이전 버전으로 되돌리지 못한다.** 롤백은 "이전 버전 재게시"가 아니라 "수정판을 더 높은 versionCode로 다시 배포"하는 것뿐이다.

| 단계 | 문제 발생 시 |
|---|---|
| Phase 1 (웹) | 서버 롤백. **즉시 복구 가능** — 그래서 웹을 먼저 한다 |
| Phase 2 (기능) | 단계적 출시 halt → 미배포 사용자 보호 → 수정판 versionCode +1 배포 |
| Phase 3 (브랜드) | halt 가능하지만 이미 바뀐 사용자는 되돌아가지 않음 → **브랜드 전환은 실질적으로 편도** |

Phase 3에 앞서 §4 커뮤니케이션을 반드시 완료해야 하는 이유가 이것이다. 되돌릴 수 없다.

**원격 킬 스위치를 하나 둔다.** Supabase에 설정 테이블을 만들고 앱이 부팅 시 조회하게 한다. 광고 on/off, 전환 안내 on/off, 점검 공지를 **앱 배포 없이** 제어할 수 있어야 한다. 프로덕션 앱에서 이게 없으면 모든 대응이 스토어 심사를 기다려야 한다.

---

## 8. 단계별 완료 기준

**Phase 0**
- [ ] 프로덕션 지표 스냅샷 확보
- [ ] `skipIntegrity` 플래그로 로컬 개발 빌드 정상 실행
- [ ] 리다이렉트 맵 작성, 서비스명 확정

**Phase 1 (웹)**
- [ ] 구버전 앱(현재 프로덕션 빌드)에서 새 웹이 정상 동작
- [ ] 구버전 앱에서 전화번호가 텍스트로 확인 가능
- [ ] 하단 탭바에 콘텐츠가 가리지 않음
- [ ] 기존 URL 404 0건
- [ ] 지역 설정 마이그레이션 동작
- [ ] 배포 후 1주 지표가 기준선 대비 악화되지 않음

**Phase 2 (기능)**
- [ ] Integrity 소프트 게이트 전환 완료 — 검증 실패 시에도 콘텐츠 열람 가능
- [ ] `skipIntegrity`가 프로덕션 빌드에 포함되지 않음
- [ ] 실기기 `tel:` 통화 연결 (Android/iOS)
- [ ] 비행기 모드 저장함 동작
- [ ] 기존 푸시 구독자에게 정상 발송
- [ ] 크래시율·ANR이 이전 버전 이하
- [ ] 전환 안내 화면이 원격 플래그로 제어됨 (아직 꺼진 상태)

**Phase 3 (브랜드)**
- [ ] D-7, D-1 카카오톡 공지 발송 완료
- [ ] 인앱 전환 안내 노출 확인
- [ ] 스토어 설명에 이전 이름 병기
- [ ] 배포 후 2주간 리뷰·문의 모니터링, "앱이 없어졌다" 유형 대응
- [ ] 삭제율이 이전 대비 유의하게 상승하지 않음

---

## 부록 A. Claude Code 투입 프롬프트

```
이 저장소는 com.jiwongum.app — 이미 Google Play와 App Store에 프로덕션 배포된
라이브 앱이다. Expo SDK 56 / RN 0.85 기반 WebView 래퍼이고,
Play Integrity API로 무결성 검증을 하고 있다.

지금은 신규 개발이 아니라 기존 앱의 대규모 리브랜딩 "업데이트"다.
사용자가 이미 있고, 되돌릴 수 없는 변경이 포함된다.

루트의 RELEASE-PLAN.md 를 먼저 읽어라. 그 다음 SPEC.md, SPEC-APP.md,
SPEC-APP-RN.md 를 읽어라. RELEASE-PLAN.md 가 배포 순서에 대해 우선한다.

[Phase 0] 지금 할 일

1. Play Integrity 현재 구현을 찾아서 보고해라.
   - Classic 요청인지 Standard 요청인지
   - 검증 실패 시 앱을 차단하는가(하드 게이트), 아니면 일부 기능만 막는가
   - 검증 호출 빈도 (실행마다? 화면마다?)
   - 사용자에게 보여주는 오류 문구 전문

2. 개발 우회 플래그를 도입해라.
   - __DEV__ 또는 EAS 프로파일 기반 skipIntegrity
   - 로컬 개발 빌드에서 Integrity 게이트가 동작하지 않아야 한다
     (디버그 서명이라 항상 UNRECOGNIZED_VERSION이 나온다)
   - production 프로파일에서는 절대 켜지지 않도록 방어

3. Integrity를 소프트 게이트로 전환해라.
   - 검증 실패 시 앱 차단이 아니라 광고만 비활성화
   - 콘텐츠 열람은 어떤 판정에서도 막지 않는다
   - 검증은 콜드 스타트 1회, 결과 24시간 캐시
   - 무결성 관련 화면을 사용자에게 노출하지 않는다

4. 현재 사이트의 전 URL을 수집해 리다이렉트 맵 초안을 만들어라.

5. 원격 설정(킬 스위치)을 Supabase에 추가해라.
   - 광고 on/off, 전환 안내 on/off, 점검 공지
   - 앱 부팅 시 1회 조회, 실패 시 안전한 기본값으로 폴백

Phase 1~3은 별도 지시가 있을 때까지 착수하지 마라.
특히 앱 이름과 아이콘은 이번 단계에서 건드리지 않는다.

제약:
- 패키지명 com.jiwongum.app, 서명키 변경 금지
- 기존 사용자의 localStorage 키와 Supabase 푸시 토큰을 보존한다
- 각 단계마다 변경 파일과 근거를 먼저 제시하고 승인을 받아라
```
